import React from 'react';
import MenuBanner from '../MenuBanner/MenuBanner';

const menu = () => {
    return (
        <div>
           <MenuBanner></MenuBanner>
        </div>
    );
};

export default menu;