import React from 'react';
import LoginPage from '../LoginForm/LoginForm';

const page = () => {
    return (
        <div>
            <LoginPage></LoginPage>
        </div>
    );
};

export default page;